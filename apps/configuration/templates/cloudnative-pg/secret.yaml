{{- range .Values.cloudnativePgClusters }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .secret.name }}
  namespace: {{ .namespace }}
type: Opaque
data:
  {{ .users.passwordSecret.key }}: {{ .secret.password | b64enc }}
---
{{- end }}
